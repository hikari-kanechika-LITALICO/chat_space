%ul.chat-groups
  %li.chat-group
    = link_to group_messages_path(group), class: "chat-group__link" do
      %p.chat-group__name= group.name
      - if group.messages.first
        %p.chat-group__message= group.messages.first.text
